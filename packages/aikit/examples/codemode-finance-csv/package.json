{
	"name": "aikit-codemode-finance-csv-example",
	"version": "0.0.0",
	"private": true,
	"description": "CodeMode finance CSV example",
	"type": "module",
	"scripts": {
		"dev": "pnpm -C ../../../.. --filter @codeworksh/utils build && pnpm -C ../../../.. --filter @codeworksh/aikit build && vp pack && node --env-file-if-exists=.env dist/index.mjs",
		"start": "pnpm -C ../../../.. --filter @codeworksh/utils build && pnpm -C ../../../.. --filter @codeworksh/aikit build && node --env-file-if-exists=.env dist/index.mjs",
		"build": "pnpm -C ../../../.. --filter @codeworksh/utils build && pnpm -C ../../../.. --filter @codeworksh/aikit build && vp pack",
		"check": "vp check",
		"lint": "vp lint"
	},
	"dependencies": {
		"@codeworksh/aikit": "link:../..",
		"typebox": "^1.1.37"
	},
	"devDependencies": {
		"@types/node": "^24.6.1",
		"typescript": "^5.8.3",
		"vite-plus": "0.1.16"
	},
	"engines": {
		"node": ">=24.14.1"
	}
}
