# @codeworksh/aikit

Shared AI kit library for the Codework monorepo.

This package is intended to hold reusable AI-facing utilities, types, and higher-level helpers that can be consumed across workspace packages and later published to npm.


Install:

```bash
npm install @codeworksh/aikit
```
