{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"incremental": false,
		"noEmit": false
	},
	// Only the packages that publish declarations; the CLI ships none (`dts: false`).
	"include": ["packages/aikit/src/**/*.ts", "packages/harness/src/**/*.ts", "packages/plugin/src/**/*.ts"]
}
