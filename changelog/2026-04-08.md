# Changelog - 2026-04-08

## Changes

- Added a follow-up `aikit` patch release to `0.2.1` with stable message IDs, ID-based message-part updates, API surface reduction, and package cleanup.
- Bootstrapped the workspace with the initial `aikit` and shared utilities packages.
- Added top-level documentation, the project logo, repository license, and `AGENTS.md`.
- Prepared the workspace for npm publishing and cleaned up package naming and dependency metadata.
- Migrated the repo from Bun/Biome-based tooling to `pnpm` + `vite-plus` with ESM-first configuration.
- Pinned the root `vite-plus` version and tightened the publish pipeline for package releases.

## Packages

### `aikit@0.2.1`

- Added generated message IDs so messages carry a stable identifier through creation and follow-up updates.
- Updated message-part mutation and update flows to target message IDs, making streamed or incremental assistant updates more reliable.
- Minimized the exposed API surface to keep package internals out of the public contract.
- Applied general package cleanup as part of the patch release.

### `aikit@0.2.0`

- Introduced the core AI toolkit with agent loops, streaming, message primitives, model catalog support, provider abstractions, and an Anthropic provider.
- Standardized runtime and stream event names to dot notation for a more consistent public API.
- Added support for async agent emit flow and improved `run` / continue-loop behavior with new tests.
- Simplified internal generic usage while keeping the public tool authoring API ergonomic, including cleaner `Message.defineTool(...)` and `Agent.defineTool(...)` patterns.
- Added a stateful `Agent.create(...)` instance API on top of the core loop.
- Fixed state persistence so tool-driven mutations continue to update assistant messages after `message.end`.
- Expanded package coverage with tests for public API behavior, instance state, loop continuation, and self-contained test paths.
- Applied Vite+-driven lint cleanup across provider and stream internals.

### `utils@0.1.1`

- Added the shared utilities package for reusable filesystem, lazy evaluation, async/runtime, and error helpers.
- Aligned package metadata and workspace setup for npm publishing as part of the monorepo release preparation.
- Carried the package through the Bun-to-Vite+ workspace migration and the package rename cleanup reflected in history.
